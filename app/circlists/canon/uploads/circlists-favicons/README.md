# Circlists card-preview favicons

5 real favicons, fetched direct from each site (not via Google), for domains seeded in
`next`'s Backend Pod / Book Club circles that don't already carry a preview image:

- `sqlite.org.ico` — sqlite.org
- `jvns.ca.ico` — jvns.ca
- `kernel.org.png` — kernel.org
- `gutenberg.org.ico` — gutenberg.org
- `newyorker.com.ico` — newyorker.com

For the Design prototype to bake in as static assets (same treatment as the card-preview
og images), so the homepage-demo build can stop calling Google's live favicon service.
