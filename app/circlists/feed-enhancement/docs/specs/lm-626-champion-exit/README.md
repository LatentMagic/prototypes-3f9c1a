# LM-626 — champion exit

Dormancy, succession, leave, account deletion, and the failing payment. The remedy for a
dormant circle belongs to every member, not to the outgoing champion.

## Contents

- [`handoff-2026-08-03_champion-exit.md`](handoff-2026-08-03_champion-exit.md) — implementation
  handoff. Carries the full change list, the state-shape additions, and an 18-row
  **surface-by-surface review list** for verifying the build one screen at a time.

- [`pg-wb-dormant.jsx`](pg-wb-dormant.jsx) + [`pg-wb-board.jsx`](pg-wb-board.jsx) — modules for
  the **whiteboard playground** (`whiteboard-playground.html` at the project root): three
  candidate organisations of the dormant screen, mounted in the real app shell. See the
  handoff's appendix for why it exists.

No `PROMPT.md`: the delta brief arrived pasted in chat rather than as a file.
