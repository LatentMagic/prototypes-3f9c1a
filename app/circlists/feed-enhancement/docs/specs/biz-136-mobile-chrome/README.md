# biz-136 — the phone's chrome

Whiteboard modules and build for the mobile-chrome exploration. The page itself
is `mobile-chrome-whiteboard.html` at the candidate build root; the standalone
export sits beside it.

`node docs/specs/biz-136-mobile-chrome/build.mjs` writes both, run from the
candidate build root.
